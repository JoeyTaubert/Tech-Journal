network:
  version: 2
  renderer: networkd
  ethernets:
    {{ device }}:
      dhcp4: no
      addresses:
        - {{ ip }}/{{ prefix }}
      gateway4: {{ gateway }}
      nameservers:
        search: [{{ domain }}]
        addresses: [{{ name_server }}]
        